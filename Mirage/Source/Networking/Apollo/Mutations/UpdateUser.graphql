mutation UpdateUser($updateUserInput: UpdateUserInput!) {
  updateUser(input: $updateUserInput) {
    id
    phone
    username
    profileImage
    profileDescription
    collected {
        id
    }
    miras {
        id
    }
  }
}
