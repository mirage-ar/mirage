query User($userInput: AuthorizedQueryInput!) {
  user(input: $userInput) {
    id
    username
    phone
    profileImage
    profileImageDesaturated
    profileDescription
    miras {
      id
    }
  }
}
