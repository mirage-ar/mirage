query User($userId: ID) {
  user(userId: $userId) {
    id
    phone
    username
    profileImage
    profileDescription
    accessToken
    verificationSid
    collected {
        id
    }
    miras {
        id
    }
  }
}
