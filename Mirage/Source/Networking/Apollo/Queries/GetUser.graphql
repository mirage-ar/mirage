query User($userId: ID) {
  user(userId: $userId) {
    id
    username
    phone
    profileImage
    profileImageDesaturated
    profileDescription
    accessToken
    verificationSid
    collected {
        id
    }
    miras {
        id
    }
  }
}
