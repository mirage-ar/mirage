query GetSuggestionsQuery($phoneNumbers: [String!]!) {
  getExistingUsers(phoneNumbers: $phoneNumbers) {
    id
    phone
    username
    profileImage
    profileDescription
    friendshipStatus
  }
}

